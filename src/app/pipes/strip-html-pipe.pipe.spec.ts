import { StripHtmlPipePipe } from './strip-html-pipe.pipe';

describe('StripHtmlPipePipe', () => {
  it('create an instance', () => {
    const pipe = new StripHtmlPipePipe();
    expect(pipe).toBeTruthy();
  });
});
